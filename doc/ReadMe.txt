# Primitive obsession
    it is an anti-pattarn where you are using primitive types 
    to represent a complex values in your domain, like use 
    integer or GUID to represent Ids property.
    instead of using primitive Id, we can use Strongly-typed Id